repos:
  - repo: local
    hooks:
      - id: data-csv-no-pii-headers
        name: CSVs under data must not list PII / demo columns
        entry: python3 scripts/check_staged_csvs.py
        language: system
        files: ^data/.*\.csv$
        pass_filenames: true
